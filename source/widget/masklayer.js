(function(Y){	/**	 * 那边去除了内嵌iframe的支持，因此masklayer也没有必要提供同等方案。所以这里不提供dialog无缝接口	 */	//默认遮罩层样式配置	var DEF_STYLE_CONFIG = {		position: 'absolute',		top: 0,		left:0,		width: '100%',		backgroundColor: '#ddd',		zIndex: 999,		opacity: 0.5	};	//遮罩层DOM	var MASKER_DOM;	/**	 * 遮罩层	 * 该对象仅提供单例模式	 */	var masklayer = {		/**		 * show masklayer		 * @param {Object} styleConfig 样式配置，支持覆盖所有key		 */		show: function(styleConfig){			if(!MASKER_DOM){				MASKER_DOM = Y.dom.create('div');				styleConfig = Y.object.extend(true, DEF_STYLE_CONFIG, styleConfig || {});				MASKER_DOM.setStyle(styleConfig);			}			var winRegion = Y.dom.getWindowRegion();			MASKER_DOM.setStyle('height',winRegion.documentHeight);			MASKER_DOM.show();		},		/**		 * 设置样式		 * @param {String} key		 * @param {String} val		 **/		setStyle: function(key, val){			DEF_STYLE_CONFIG[key] = val;		},		/**		 * hide masklayer		 */		hide: function(){			if(MASKER_DOM){				MASKER_DOM.hide();			}		}	};	Y.widget.masklayer = masklayer;})(YSL);